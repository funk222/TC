#ifndef RECEIVER_SECRETS_H
#define RECEIVER_SECRETS_H

// Copy this file to src/receiver_secrets.h and fill in your private values.
// src/receiver_secrets.h is git-ignored and will not be committed.

#define RECEIVER_WIFI_SSID "YOUR_REAL_WIFI_SSID"
#define RECEIVER_WIFI_PASSWORD "YOUR_REAL_WIFI_PASSWORD"

#endif // RECEIVER_SECRETS_H
